\documentclass{beamer}

\usepackage{préambule}

\begin{document}

\newcommand{\myStartX}{-5}
\newcommand{\myEndX}{8}
\newcommand{\myStartY}{-5}
\newcommand{\myEndY}{5}

\begin{frame}
	\begin{center}
		\begin{tikzpicture}[scale=0.6]
			\foreach \y in {\myStartY, ..., \myEndY} {
					\draw[thin,gray] (\myStartX,\y) -- (\myEndX,\y);
					\ifthenelse{\equal{\y}{0}}{}{
						\draw[thick] (-0.2,\y) node[left] {\y} -- (0,\y);
					}
				}
			\foreach \x in {\myStartX, ..., \myEndX} {
					\draw[thin,gray] (\x,\myStartY) -- (\x,\myEndY);
					\ifthenelse{\equal{\x}{0}}{}{
						\draw[thick] (\x,-0.2) node[below] {\x} -- (\x,0);
					}
				}
			\node[below,left] at (-0.2,-0.3) {$0$};
			\draw[very thick,\myArrow] (\myStartX - 0.3,0) -- (\myEndX + 0.5,0);
			\draw[very thick,\myArrow] (0,\myStartY - 0.3) -- (0,\myEndY + 0.5);
		\end{tikzpicture}
	\end{center}
\end{frame}

\end{document}